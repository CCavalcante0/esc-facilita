# Supabase — projeto "ESC FACILITA"
# Copie para .env.local e preencha. NUNCA commitar o .env.local (já ignorado).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Secret key (sb_secret_...) — NÃO usada pela Fase 1 (o site só insere leads).
# Necessária apenas na Fase 3 (leitura de leads no backoffice).
# SUPABASE_SECRET_KEY=
